{
  "name": "quickchat-fullstack-root",
  "version": "1.0.0",
  "description": "Root package for QuickChat Full Stack",
  "main": "server/server.js",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {}
}
